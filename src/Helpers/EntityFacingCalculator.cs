using System.Numerics;

namespace GungeonClone.src.Helpers
{
    public static class EntityFacingCalculator
    {
        //public static Vector2 GetDirection(Vector2 entity, Vector2 entity2)
        //{
        //    if (entity2.X > entity.X && entity2.Y > entity.Y)
        //    {

        //    }
        //    else if (entity2.X < entity.X && entity2.Y > entity.Y)
        //    {

        //    }
        //    else if (entity2.X > entity.X && entity2.Y < entity.Y)
        //    {

        //    }
        //    else if (entity2.X < entity.X && entity2.Y < entity.Y)
        //    {

        //    }
        //}
    }
}
